# Program to print the first n terms of an arithmetic series
a = int(input("Enter the first term (a): "))
d = int(input("Enter the common difference (d): "))
n = int(input("Enter the number of terms (n): "))

print("Arithmetic series:")
for i in range(n):
    term = a + i * d
    print(term, end=" ")
